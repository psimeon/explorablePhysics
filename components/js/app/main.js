// Declare app level module which depends on views, and components
'use strict';

(function(){
	var app = angular.module('expPhys',[]);
	app.controller('moduleCtrl',['$scope', function($scope){

	}]);
})();